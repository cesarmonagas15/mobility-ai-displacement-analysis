services:
  - type: web
    name: mobility-ai-dashboard
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: python src/dashboard/interactive_dashboard.py
    envVars:
      - key: PORT
        value: 8050
      - key: HOST
        value: 0.0.0.0

